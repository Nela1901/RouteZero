# 12. Modelo C4 — Arquitectura de Software

## Metadatos

| Campo | Detalle |
|---|---|
| **Nombre del Proyecto** | RouteZero — Optimizador de rutas sostenibles |
| **Empresa ficticia** | Andina Reparto S.A.C. |
| **Integrantes** | Inciso Aguilar Elizabeth Antonela, Espinoza Tiza Yago Imanol, Guerra Lozano Keen, Uscuvilca Ramos Abraham Luis |
| **Fecha** | 2026-09-05 |
| **Versión** | 1.0.0 |

## 1. Nivel 1 — Diagrama de Contexto

Muestra el sistema RouteZero en relación con los usuarios externos y sistemas externos con los que interactúa.

```mermaid
graph TD
    Admin([Administrador de operaciones])
    Operador([Operador de despacho])
    Conductor([Conductor de reparto])
    Gerente([Gerente general])

    System[🌿 RouteZero\nOptimizador de rutas sostenibles\nPython + FastAPI + React + PostgreSQL]

    OSM[OpenStreetMap\nServicio de mapas gratuito]
    Render[Render\nInfraestructura cloud backend]
    Vercel[Vercel\nInfraestructura cloud frontend]

    Admin -->|Gestiona flota, pedidos y genera rutas| System
    Operador -->|Registra pedidos y clientes| System
    Conductor -->|Visualiza rutas asignadas| System
    Gerente -->|Consulta dashboard y reportes| System

    System -->|Obtiene datos geográficos del distrito de Huancayo| OSM
    System -->|Despliega API y base de datos| Render
    System -->|Despliega interfaz web| Vercel
```

## 2. Nivel 2 — Diagrama de Contenedores

Muestra los contenedores principales del sistema RouteZero y cómo se comunican entre sí.

```mermaid
graph TD
    subgraph Usuarios
        Admin([Administrador])
        Conductor([Conductor])
        Gerente([Gerente])
    end

    subgraph RouteZero
        SPA[Single Page Application\nReact.js 18+\nVercel]
        API[API Backend\nPython + FastAPI\nRender]
        DB[(Base de Datos\nPostgreSQL 15+\nRender)]
        ALG[Motor de Optimización\nACO + GA\nPython]
    end

    subgraph Externos
        OSM[OpenStreetMap API]
    end

    Admin -->|HTTPS / Browser| SPA
    Conductor -->|HTTPS / Browser móvil| SPA
    Gerente -->|HTTPS / Browser| SPA

    SPA -->|REST / JSON HTTPS| API
    API -->|SQL / asyncpg| DB
    API -->|Llama internamente| ALG
    ALG -->|Lee y escribe resultados| DB
    SPA -->|Tiles de mapa HTTPS| OSM
```

## 3. Nivel 3 — Diagrama de Componentes

Muestra la estructura interna del contenedor API Backend (FastAPI), detallando sus módulos principales.

```mermaid
graph TD
    subgraph API Backend - FastAPI
        MW[Middleware de Seguridad\nJWT Auth + CORS + Rate Limiting]

        subgraph Controladores - Routers
            RC[Router Flota\n/api/vehiculos]
            RP[Router Pedidos\n/api/pedidos]
            RR[Router Rutas\n/api/rutas]
            RU[Router Usuarios\n/api/usuarios]
            RD[Router Dashboard\n/api/dashboard]
            REP[Router Reportes\n/api/reportes]
        end

        subgraph Servicios - Lógica de Negocio
            SF[Servicio Flota]
            SP[Servicio Pedidos]
            SR[Servicio Rutas]
            SA[Servicio Algoritmo ACO + GA]
            SS[Servicio Sostenibilidad]
        end

        subgraph Repositorios - Acceso a Datos
            RF2[Repositorio Flota]
            RP2[Repositorio Pedidos]
            RR2[Repositorio Rutas]
            RM[Repositorio Métricas]
        end

        DB[(PostgreSQL)]
    end

    SPA[React.js Frontend] -->|REST JSON| MW
    MW --> RC & RP & RR & RU & RD & REP

    RC --> SF --> RF2
    RP --> SP --> RP2
    RR --> SR --> SA
    SA --> RR2
    RD & REP --> SS --> RM

    RF2 & RP2 & RR2 & RM -->|SQLAlchemy + asyncpg| DB
```

## 4. Nivel 4 — Diagrama de Código (Motor de Optimización ACO)

Muestra la estructura interna del componente crítico del sistema: el motor de optimización ACO.

```mermaid
graph TD
    subgraph Motor ACO - VRPTW + Green VRP
        IN[Input\nPedidos + Vehículos + Restricciones]
        
        subgraph Inicialización
            FER[Matriz de Feromonas\nInicialización uniforme]
            HEU[Matriz Heurística\nDistancia + Ventanas de tiempo]
        end

        subgraph Ciclo de Hormigas
            CH[Construcción de Solución\nSelección probabilística]
            EV[Evaluación\nDistancia + CO₂ + Penalizaciones]
            AC[Actualización de Feromonas\nEvaporación + Depósito]
        end

        subgraph Criterio de Parada
            IT[¿Iteraciones completadas?\no ≤ 45 segundos]
        end

        OUT[Output\nRutas óptimas por vehículo]
    end

    IN --> FER & HEU
    FER & HEU --> CH
    CH --> EV --> AC --> IT
    IT -->|No| CH
    IT -->|Sí| OUT
```